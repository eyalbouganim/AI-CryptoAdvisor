import React from 'react';
import {
    Typography,
    Box,
} from '@mui/material';

const InsightAI = () => {
  return (
    <Box sx={{ p: 2, border: '1px dashed grey', height: '100%' }}>
      <Typography variant="h6">Insight AI</Typography>
    </Box>
  );
};

export default InsightAI;